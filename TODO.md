# Remaining OMO work

Only unfinished tasks are listed. Implementation history belongs in CHANGELOG.md. Paid work, scheduling and model promotion are disabled.

| ID | Priority / state | Task and acceptance criteria | Dependencies / blocker |
|---|---|---|---|
| OMO-001 | P0 / verification pending | Pass CI on the actual PR head; native AMD64/ARM64 bundled-image offline answer/helper/sandbox/auth/shutdown tests; resolve container findings and record image size/digests. | Remote Actions runs; local workspace has no Docker daemon. |
| OMO-002 | P0 / blocked release | Independent sandbox review and broader native-parser adversarial cases; retain flag/private deployment until evidence supports broader exposure. Verify container OS/native dependency vulnerability scan. | Review/scan evidence, no claim of VM-grade isolation. |
| OMO-003 | P1 / provisional | Compare 360M with Granite/LFM/Qwen and a Transformers CPU reference; evaluate Q4/Q8 after quantization. Predeclare broader quality, latency and incorrect-local-answer gates; expand exact local envelope only on evidence. | Current evidence is four independent local prompts, no paid sweep approved. |
| OMO-004 | P1 / planned | Improve routing task labels and validated model-produced helper arguments; evaluate held-out helper selection, argument fidelity and result interpretation; measure routing contention. | Outcome dataset and calibration; no autonomous guest code at present. |
| OMO-005 | P1 / planned | Native upstream SSE with bounded event parser, pre-first-chunk recovery, midstream failure/cancellation tests and token accounting; preserve privacy/cost on every retry. | Current implementation deliberately buffers complete answers. |
| OMO-006 | P1 / blocked account | Establish HF repository publishing authority/trusted publisher claims, create model/dataset resources after exact identity/license checks, upload original dataset with immutable revisions; do not relabel base weights as fine-tune. | Connector has no publishing scope; reported credential expiry needs refresh/verification. |
| OMO-007 | P0 / blocked stable publication | Confirm Docker namespace/token via Actions; configure protected release environment; add verified SBOM/vulnerability/secret scans, signatures, multi-platform stable promotion and complete rerunnable release/rollback manifest. Stable aliases must advance only after both candidate digests pass. | Owner environment protection and approved main commit/version. Candidate-only workflow exists; stable promotion/signing not implemented. |
| OMO-008 | P1 / planned, execution disabled | Implement isolated locked Transformers SFT/export/quantization path; local tiny smoke, full-vs-adapter comparison, protected held-out gates, lineage and rollback. | Current trainer is a validated dry-run planner only. Paid budget/operation not approved. |
| OMO-009 | P1 / planned | Enlarge original datasets with family-level deduplication, source/licensing audit, outcome schema validation, calibrated selective risk/coverage, fixed-provider baselines and human-reviewed quality. | RouterBench exact artifact licenses unresolved; no live-provider evaluation budget. |
| OMO-010 | P2 / planned | Robust multi-tenant auth/authorization, per-caller budgets/retention, DNS/IP egress enforcement, observed cost reconciliation and bounded telemetry export. | Current single-key private deployment; admin-configured endpoints are trusted. |
| OMO-011 | P2 / planned | Offline registry discovery/refresh with freshness thresholds, tokenizer-specific external counts and last-known-good reload administration. | Existing reload is internal Python API; no request-time discovery or downloads. |
