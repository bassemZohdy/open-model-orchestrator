# Remaining OMO work

Only unfinished tasks are listed. Implementation history belongs in CHANGELOG.md. Paid work, scheduling and model promotion are disabled.

| ID | Priority / state | Task and acceptance criteria | Dependencies / blocker |
|---|---|---|---|
| OMO-012 | P1 / blocked account | Validate issue-first Codex development followed by automatic Claude review on an actual code PR. | Codex reported missing OMO cloud environment on PR #3. Owner setup needed; bot-triggered review remains unverified. Disable duplicate automatic Codex reviews in owner settings if enabled. |
| OMO-007 | P0 / blocked stable publication | Configure protected release environment; resolve DiskCache CVE-2025-69872 strict-audit blocker; verify candidate SBOM/provenance and strict vulnerability gates; add signatures and execute the evidence-bound multi-platform stable promotion with rerunnable rollback manifests. Stable aliases must advance only after both candidate digests pass. | Docker login verified as bzohdy; owner environment protection, signing authority and approved main commit/version remain. Candidate manifests and promotion validation exist; publication is still blocked. |
| OMO-002 | P0 / blocked release | Complete independent sandbox review and broader malformed native-parser review; resolve remaining container HIGH/CRITICAL findings, including entries without fixed versions, or record an owner-approved VEX decision. Retain flag/private deployment until gates pass. | Security report validation and adversarial regression coverage now exist; native Trivy findings, clean-audit status and VM-grade isolation remain unverified. |
| OMO-003 | P1 / provisional | Run the pinned candidate comparison for 360M, Granite, LFM and Qwen plus a Transformers CPU reference; evaluate Q4/Q8 only after the reference comparison and expand the exact local envelope only on evidence. | The candidate set and broader quality/latency/incorrect-answer gates are predeclared; no comparison sweep or paid provider evaluation is approved. |
| OMO-004 | P1 / provisional | Evaluate held-out helper selection, argument fidelity, result interpretation and routing contention; only expand helper coverage after protected evidence and calibration. | Typed model-proposed helper arguments are now validated by the same schemas as explicit calls; outcome calibration and held-out evidence are still missing. |
| OMO-006 | P1 / blocked account | Obtain HF publishing authority/trusted-publisher claims, create model/dataset resources after exact identity/license checks, and upload the original dataset with immutable revisions; do not relabel base weights as a fine-tune. | A disabled publication identity contract now binds the upstream model and dataset; the connector has no publishing scope and no upload was attempted. |
| OMO-008 | P1 / planned, execution disabled | Implement isolated locked Transformers SFT/export/quantization path; local tiny smoke, full-vs-adapter comparison, protected held-out gates, lineage and rollback. | Current trainer is a validated dry-run planner only. Paid budget/operation not approved. |
| OMO-009 | P1 / planned | Enlarge original datasets with family-level deduplication, source/licensing audit, outcome schema validation, calibrated selective risk/coverage, fixed-provider baselines and human-reviewed quality. | RouterBench exact artifact licenses unresolved; no live-provider evaluation budget. |
| OMO-010 | P2 / planned | Robust multi-tenant auth/authorization, per-caller budgets/retention, DNS/IP egress enforcement, observed cost reconciliation and bounded telemetry export. | Current single-key private deployment; admin-configured endpoints are trusted. |
| OMO-011 | P2 / planned | Offline registry discovery/refresh with freshness thresholds, tokenizer-specific external counts and last-known-good reload administration. | Existing reload is internal Python API; no request-time discovery or downloads. |
