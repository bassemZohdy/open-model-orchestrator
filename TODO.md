# Remaining OMO work

Only unfinished work is listed here. Completed implementation history belongs in
`CHANGELOG.md`. Paid training/inference, external publication, scheduling and
automatic promotion remain disabled.

| ID | Priority / state | Task and acceptance criteria | Dependencies / blocker |
|---|---|---|---|
| OMO-007 | P0 / blocked stable publication | Configure the protected release environment; resolve the DiskCache CVE-2025-69872 audit blocker; verify candidate SBOM/provenance and strict vulnerability gates; add signatures and execute evidence-bound multi-platform stable promotion with rerunnable rollback manifests. Stable aliases advance only after both candidate digests pass. | Owner environment protection, signing authority, approved main commit/version, dependency remediation or reviewed VEX, and remaining native findings are required. |
| OMO-002 | P0 / blocked release | Complete independent sandbox review and broader malformed native-parser review; resolve remaining container HIGH/CRITICAL findings, including entries without fixed versions, or record an owner-approved VEX decision. Retain private deployment restrictions until gates pass. | Adversarial regression coverage exists; independent review, clean-audit status and VM-grade isolation remain unverified. |
| OMO-003 | P1 / provisional | Run the pinned candidate comparison for 360M, Granite, LFM and Qwen plus a Transformers CPU reference; evaluate Q4/Q8 only after the reference comparison and expand the exact local envelope only on evidence. | The 30-record original synthetic corpus and evaluation gates are defined, but no comparison sweep or paid provider evaluation is approved. |
| OMO-004 | P1 / provisional | Evaluate held-out helper selection, argument fidelity, result interpretation and routing contention; expand helper coverage only after protected evidence and calibration. | Typed model-proposed helper arguments are validated; outcome calibration and held-out evidence are still missing. |
| OMO-006 | P1 / blocked account | Obtain HF publishing authority/trusted-publisher claims, create model/dataset resources after exact identity/license checks, and upload original data with immutable revisions. Do not relabel base weights as a fine-tune. | Publication identity checks exist and remain disabled; the connector has no publishing scope and no upload was attempted. |
| OMO-008 | P1 / partial; execution disabled | Execute an owner-approved isolated Transformers SFT/export/quantization run; compare full and adapter outputs, evaluate protected held-out gates, and publish lineage plus rollback manifests only after evidence passes. | Offline locked preparation, dataset lineage, quantization targets and rollback requirements are implemented. Transformers execution, export, quantization and any paid operation remain disabled. |
| OMO-009 | P1 / partial; provider and human review blocked | Collect fixed-provider baselines, calibrate selective risk/coverage, complete source/license audit and human-reviewed quality assessment on an enlarged corpus. | Original synthetic corpus expansion, family/content deduplication and outcome-schema validation are implemented. Live-provider baselines and human review are not approved. |
| OMO-010 | P2 / partial; deployment follow-up | Move caller budget accounting to a durable multi-instance store, add measured cost reconciliation and enforce DNS/IP egress through deployment network policy before multi-tenant production use. | Hash-based caller identity, tenant/role policy, exact host allowlists, retention filtering, per-request limits and bounded in-memory daily reservations are implemented. |
| OMO-011 | P2 / partial; offline only | Add an owner-controlled registry source and refresh administration with authenticated deployment integration; add provider-specific tokenizer adapters only when independently verified. | Local registry status, freshness threshold, last-known-good reload and a declared tokenizer strategy are implemented. Request-time discovery/downloads remain disabled. |
