# Open Model Orchestrator (OMO)

A self-contained Python agent with embedded model inference, controlled computation, and policy-enforced external routing.

Implementation is being developed on `feat/bootstrap-v0.1`. This initial commit establishes the main branch for review.
